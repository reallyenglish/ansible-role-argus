argus_user: "{{ __argus_user }}"
argus_group: "{{ __argus_group }}"
argus_log_dir: /var/log/argus
argus_service: "{{ __argus_service }}"
argus_package: "{{ __argus_package }}"
argus_client_package: "{{ __argus_client_package }}"
argus_conf_dir: "{{ __argus_conf_dir }}"
argus_conf_file: "{{ __argus_conf_dir }}/argus.conf"
argus_pid_dir: "{{ __argus_pid_dir }}"
argus_pid_file: "{{ __argus_pid_dir }}/{{ argus_service }}.pid"

argus_saslpassword_command: "{{ __argus_saslpassword_command }}"
argus_sasldblistusers_command: "{{ __argus_sasldblistusers_command }}"
argus_sasldb_file: "{{ __argus_sasldb_file }}"

argus_flags: ""

argus_sasl_user: {}
argus_config: {}
